spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/travel_agency}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.config.import=optional:file:.env[.properties]

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dnxtihowp}
cloudinary.api-key=${CLOUDINARY_API_KEY:977412445636831}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:_EeVY8KFyvPnAbOPa92xzr0dGss}

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB

jwt.secret=${JWT_SECRET:clave_secreta_temporal_para_desarrollo_local}
jwt.expiration=86400000

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.from=${MAIL_FROM:no-reply@example.com}

spring.mail.properties.mail.debug=true